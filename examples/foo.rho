struct Pow {
    var x int;
    var n int;

    fun run() int {
        var i int = 1;
        var res int = 1;

        while i <= pow.n {
            res *= pow.x;
            i += 1;
        }

        return res;
    }
}

fun square(x int) int {
    return Pow{.x = x, .n = 2}.run();
}
