struct A {
    a int,
    b int,
}

fun (a A) getSum() int {
    return a.a + a.b;
}

fun aFromOne(a int) A {
    return A {.a = a, .b = a};
}

fun foo(x int) int {
    var s A = aFromOne(x);
    s.b = 42;
    return s.getSum();
}
