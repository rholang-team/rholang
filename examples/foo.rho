struct Pow {
    x int,
    n int
}

fun (pow Pow) run() int {
    var i int = 1;
    var res int = 1;

    while i <= pow.n {
        res *= pow.x;
        i += 1;
    }
}

fun square(x int) int {
    return Pow {.x = x, .n = 2}.run();
}
